# # ═══════════════════════════════════════════════════════════════
# # 🧪 E2E TEST ENVIRONMENT (Prod Mirror)
# # ═══════════════════════════════════════════════════════════════
# # Tüm servisler prod ile aynı config'de, izole Docker network'ünde
# #
# # Kullanım:
# #   docker-compose -f docker-compose.e2e.yml --env-file .env.e2e up -d
# #   docker-compose -f docker-compose.e2e.yml --env-file .env.e2e down -v
# # ═══════════════════════════════════════════════════════════════

# version: "3.8"

# name: vex-e2e

# services:
#   # ─────────────────────────────────────────────
#   # 🦛 HIPPO - PostgreSQL 16 + pgvector (Main DB)
#   # ─────────────────────────────────────────────
#   hippo:
#     image: pgvector/pgvector:pg18
#     container_name: vex-e2e-hippo
#     hostname: hippo
#     restart: unless-stopped
#     environment:
#       POSTGRES_USER: ${POSTGRES_USER:-vex}
#       POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-vex_e2e_test}
#       POSTGRES_DB: ${POSTGRES_DB:-vex}
#     command: >
#       postgres
#       -c max_connections=200
#       -c shared_buffers=256MB
#       -c idle_in_transaction_session_timeout=30000
#     volumes:
#       - hippo_data:/var/lib/postgresql/data
#     networks:
#       - vex-e2e-net
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-vex}"]
#       interval: 5s
#       timeout: 5s
#       retries: 5

#   # ─────────────────────────────────────────────
#   # 🍒 CHERRY - Redis 7.2 (Cache)
#   # ─────────────────────────────────────────────
#   cherry:
#     image: redis:7.2-alpine
#     container_name: vex-e2e-cherry
#     hostname: cherry
#     restart: unless-stopped
#     command: >
#       sh -c "redis-server
#       --appendonly yes
#       --requirepass ${REDIS_PASSWORD:-e2e_redis_pass}"
#     volumes:
#       - cherry_data:/data
#     networks:
#       - vex-e2e-net
#     healthcheck:
#       test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-e2e_redis_pass}", "ping"]
#       interval: 5s
#       timeout: 3s
#       retries: 5

#   # ─────────────────────────────────────────────
#   # 🍑 PEACH - MinIO S3 Storage
#   # ─────────────────────────────────────────────
#   peach:
#     image: minio/minio:RELEASE.2024-12-18T13-15-44Z
#     container_name: vex-e2e-peach
#     hostname: peach
#     restart: unless-stopped
#     environment:
#       MINIO_ROOT_USER: ${MINIO_ROOT_USER:-vex}
#       MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-changeme}
#     command: server /data --console-address ":9001"
#     ports:
#       - "9000:9000"
#       - "9001:9001"
#     volumes:
#       - peach_data:/data
#     networks:
#       - vex-e2e-net
#     healthcheck:
#       test: ["CMD", "mc", "ready", "local"]
#       interval: 10s
#       timeout: 20s
#       retries: 5

#   # ─────────────────────────────────────────────
#   # 🍑 PEACH INIT - Bucket Setup
#   # ─────────────────────────────────────────────
#   peach-init:
#     image: minio:latest
#     container_name: vex-e2e-peach-init
#     depends_on:
#       peach:
#         condition: service_healthy
#     restart: "no"
#     entrypoint: >
#       /bin/sh -c "
#       mc alias set local http://peach:9000 ${MINIO_ROOT_USER:-vex} ${MINIO_ROOT_PASSWORD:-changeme};
#       mc mb --ignore-existing local/chrry-chat-files;
#       mc mb --ignore-existing local/chrry-app-profiles;
#       mc mb --ignore-existing local/chrry-installs;
#       mc mb --ignore-existing local/chrry-private;
#       mc mb --ignore-existing local/chrry-user-files;
#       mc mb --ignore-existing local/chrry-tribe-files;
#       mc anonymous set download local/chrry-chat-files;
#       mc anonymous set download local/chrry-app-profiles;
#       echo '✅ Peach (MinIO) buckets ready';
#       exit 0;
#       "
#     networks:
#       - vex-e2e-net

#   # ─────────────────────────────────────────────
#   # 🔷 VEX - FalkorDB Knowledge Graph
#   # ─────────────────────────────────────────────
#   vex:
#     image: falkordb/falkordb:latest
#     container_name: vex-e2e-vex
#     hostname: vex
#     restart: unless-stopped
#     volumes:
#       - vex_data:/data
#     networks:
#       - vex-e2e-net
#     healthcheck:
#       test: ["CMD", "redis-cli", "-p", "6379", "ping"]
#       interval: 5s
#       timeout: 3s
#       retries: 5

#   # ─────────────────────────────────────────────
#   # 📧 MAILHOG - Email Testing
#   # Web UI: http://localhost:8025
#   # SMTP: localhost:1025
#   # ─────────────────────────────────────────────
#   mailhog:
#     image: mailhog/mailhog:latest
#     container_name: vex-e2e-mailhog
#     hostname: mailhog
#     restart: unless-stopped
#     ports:
#       - "1025:1025"
#       - "8025:8025"
#     networks:
#       - vex-e2e-net

#   # ─────────────────────────────────────────────
#   # 🦠 MALWARE-SCANNER (Optional - E2E'de kapalı)
#   # ─────────────────────────────────────────────
#   # malware-scanner:
#   #   build:
#   #     context: ../malwareScanner
#   #     dockerfile: Dockerfile
#   #   container_name: vex-e2e-malware-scanner
#   #   environment:
#   #     - NODE_ENV=production
#   #     - PORT=3006
#   #   networks:
#   #     - vex-e2e-net
#   #   healthcheck:
#   #     test: ["CMD", "nc", "-z", "localhost", "3006"]
#   #     interval: 30s
#   #     timeout: 10s
#   #     retries: 10
#   #     start_period: 180s

#   # ─────────────────────────────────────────────
#   # 🚀 API - Backend Service
#   # ─────────────────────────────────────────────
#   api:
#     build:
#       context: ../..
#       dockerfile: apps/api/Dockerfile
#     container_name: vex-e2e-api
#     hostname: api
#     restart: unless-stopped
#     ports:
#       - "3001:3002"
#     env_file:
#       - .env.e2e
#     environment:
#       NODE_ENV: e2e
#       PORT: "3002"
#       HOSTNAME: "0.0.0.0"
#       # Override internal URLs
#       DATABASE_URL: "postgresql://${POSTGRES_USER:-vex}:${POSTGRES_PASSWORD:-vex_e2e_test}@hippo:5432/${POSTGRES_DB:-vex}"
#       DB_URL: "postgresql://${POSTGRES_USER:-vex}:${POSTGRES_PASSWORD:-vex_e2e_test}@hippo:5432/${POSTGRES_DB:-vex}"
#       DB_PROD_URL: "postgresql://${POSTGRES_USER:-vex}:${POSTGRES_PASSWORD:-vex_e2e_test}@hippo:5432/${POSTGRES_DB:-vex}"
#       REDIS_URL: "redis://:${REDIS_PASSWORD:-e2e_redis_pass}@cherry:6379/0"
#       S3_INTERNAL_ENDPOINT: "http://peach:9000"
#       S3_ENDPOINT: "http://peach:9000"
#       S3_PUBLIC_URL: "http://localhost:9000"
#       FALKORDB_HOST: "vex"
#     networks:
#       - vex-e2e-net
#     depends_on:
#       hippo:
#         condition: service_healthy
#       cherry:
#         condition: service_healthy
#       peach-init:
#         condition: service_completed_successfully
#       vex:
#         condition: service_healthy
#     healthcheck:
#       test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3002/api/health"]
#       interval: 10s
#       timeout: 5s
#       retries: 10
#       start_period: 60s

#   # ─────────────────────────────────────────────
#   # ⚡ FLASH - Frontend SSR
#   # ─────────────────────────────────────────────
#   chrry:
#     build:
#       context: ../..
#       dockerfile: apps/chrry/Dockerfile
#     container_name: vex-e2e-chrry
#     hostname: chrry
#     restart: unless-stopped
#     ports:
#       - "3000:5173"
#     env_file:
#       - .env.e2e
#     environment:
#       NODE_ENV: e2e
#       PORT: "5173"
#       HOST: "0.0.0.0"
#       # API internal communication
#       API_INTERNAL_URL: "http://api:3002"
#     networks:
#       - vex-e2e-net
#     depends_on:
#       api:
#         condition: service_healthy
#     healthcheck:
#       test: ["CMD", "wget", "-qO-", "http://127.0.0.1:5173/api/health"]
#       interval: 10s
#       timeout: 5s
#       retries: 5
#       start_period: 30s

# volumes:
#   hippo_data:
#     driver: local
#   cherry_data:
#     driver: local
#   peach_data:
#     driver: local
#   vex_data:
#     driver: local

# networks:
#   vex-e2e-net:
#     driver: bridge
#     name: vex-e2e-net
